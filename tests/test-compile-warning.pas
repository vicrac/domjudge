(*
 * This should give a compiler warning and fail with NO-OUTPUT,WRONG-ANSWER
 *
 * @EXPECTED_RESULTS@: NO-OUTPUT,WRONG-ANSWER
 *)

program warning(input, output);

var
   a : integer;

begin
   a := 1;
end.
